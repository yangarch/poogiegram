# poogiegram — 개인 미디어 라이브러리 서버

> 상태: **설계 논의 중 (v4 draft — 하드웨어·스토리지 확정)** · 최종 수정 2026-08-04
> 이 문서가 확정되기 전까지 코드는 작성하지 않는다.
> 표기: **[확정]** 합의된 결정 · **[제안]** 내 추천안, 이견 없으면 확정 · **[열림]** 논의 필요

---

## 1. 목적

개인이 소유한 사진·동영상을 **자체 서버에 보관**하고, **웹 브라우저로 열람·재생**하는 서비스.
외부 클라우드(구글 포토, iCloud)에 의존하지 않고 원본을 직접 통제하는 것이 1차 목표.

### 비목표 (이번 범위 아님)
- 사진 편집(보정, 크롭, 필터)
- 모바일 네이티브 앱 — 웹(PWA 수준)으로 대응
- 얼굴 인식·AI 자동 태깅 — v2 이후 검토
- 불특정 다수 대상 서비스, 과금

---

## 2. 결정된 전제 **[확정]**

| 항목 | 결정 |
|---|---|
| 사용자 규모 | 가족·지인 소수 공유 (계정 5~10개 내외) |
| 배포 환경 | 집/사무실 로컬 서버·NAS |
| 스택 | React (웹) + Python / FastAPI (API·미디어 파이프라인) |
| MVP 범위 | 타임라인+썸네일 그리드 / 동영상 스트리밍 / 앨범·태그 / 검색(날짜·위치·파일명) |
| 서버 하드웨어 | Intel 8세대(Coffee Lake) 6코어 / RAM 16GB / Ubuntu |
| 스토리지 | 내장 NVMe(파생물) + 외장 HDD(원본) — 2계층 (§4.0) |
| 기존 데이터 | **없음 — 백지에서 시작** (마이그레이션 도구 불필요) |
| 예상 규모 | 수천 ~ 수만 장 |
| 소스 기기 | **최신 아이폰 Pro 위주** (HEIF / HEVC / Dolby Vision) + 스냅사진 원본(대용량) |
| **입력 포맷** | **HEIC / JPEG / HEVC MOV 세 가지.** RAW는 범위 제외 (§4.5) |
| 라이브 포토 | **살린다** — 정지컷+MOV 페어링, 길게 눌러 모션 재생 (§6.5) |
| UI 톤 | 갤러리형 — 여백 있고 차분하게, 감상 중심 |

이 전제에서 파생되는 성격:
- 동시 접속자가 적다 → **수평 확장·마이크로서비스 불필요.** 단일 서버 + Docker Compose로 충분.
- 로컬 디스크가 싸다 → **원본 무손실 보관**이 기본. 파생물(썸네일 등)도 넉넉히 캐시.
- 외부 노출은 최소한 → 인터넷 공개 대신 **터널링/VPN** 경유를 기본으로.
- 백지 시작 → **인제스트 경로가 하나뿐**이다. 기존 폴더 스캔·경로 동기화 문제가 통째로 사라진다.
- 수만 장 규모 → 단일 PostgreSQL로 여유롭다. 다만 타임라인 가상 스크롤은 여전히 필수.

### 2.1 아이폰 중심이라는 전제가 바꾸는 것 **[중요]**

> 초기 설계는 "안드로이드 위주 = 대부분 H.264/JPEG"를 전제로 **변환을 예외 경로**로 잡았다.
> 최신 아이폰 Pro 위주로 정정되면서 **이 전제가 뒤집힌다.** 변환은 상시 주 경로다.

| 사실 | 설계 귀결 |
|---|---|
| 아이폰 기본 포맷이 **HEIC/HEIF** | **크롬·파이어폭스는 HEIC를 표시하지 못한다.** 사진마다 JPEG/WebP 파생물 생성이 **필수**. 선택이 아니다 |
| 아이폰 동영상이 **HEVC** | 브라우저 재생 호환성이 낮다 → 트랜스코딩이 예외가 아닌 기본 경로 |
| 최신 Pro 모델 영상은 **Dolby Vision HDR** | 그냥 SDR로 변환하면 **색이 빠지고 허옇게 뜬다.** 톤매핑 필터 필수 (§6.3) |
| **라이브 포토** = HEIC + MOV 쌍 | 별개 asset 2개로 처리하면 타임라인에 영상이 중복으로 뜬다. 페어링 필요 (§6.5) |
| 48MP HEIF Max / ProRAW 옵션 | 장당 5~8MB, ProRAW는 ~75MB. 용량 추정을 다시 잡아야 한다 (§4.4) |
| **스냅사진 원본** (전문가 납품본) | 장당 20~80MB. 단순 업로드로는 부족 → **청크 업로드** 필요 (§6.6) |

**결론: HW 가속의 중요도가 "있으면 좋음"에서 "사실상 필수"로 올라간다.**
Q6(CPU 세대)가 후순위 질문에서 **선순위**로 이동한다.

---

## 3. 전체 구조 **[확정]** — 기존 nginx에 편입

**서버에는 이미 호스트 nginx가 80/443을 점유하고 있다**(certbot으로 TLS 운영 중).
따라서 Caddy를 새로 띄우지 않고 **기존 nginx를 리버스 프록시로 재사용**한다.

이건 타협이 아니라 개선이다 — **`X-Accel-Redirect`는 원래 nginx의 기능**이라,
아래 핵심 원칙이 가장 자연스럽게 구현된다.

```
                        ┌──────────────────────────────┐
   브라우저 ──── 443 ──▶│  호스트 nginx (기존)          │
   (React SPA)          │  TLS(certbot) · 기존 사이트들 │
                        └──┬────────────────────┬──────┘
                           │ proxy_pass         │ internal location
                           │ 127.0.0.1:8005     │ /_media/ → /mnt/media/
                           │                    │ /_derived/ → …/derived/
              ┌────────────▼──────────┐         │
              │  FastAPI (컨테이너)    │         ▼
              │  인증·권한·검색·업로드 │   ┌──────────────────┐
              └──┬─────────────────┬──┘   │  파일을 nginx가   │
                 │                 │      │  직접 전송        │
    ┌────────────▼───┐   ┌─────────▼────┐ └──────────────────┘
    │  PostgreSQL 17 │   │    Redis     │
    │  (내부 전용)    │   │  (내부 전용)  │
    └────────────────┘   └──────┬───────┘
                                │
                       ┌────────▼─────────┐
                       │   Worker (arq)   │
                       │ EXIF·썸네일·변환  │
                       └────────┬─────────┘
                                │ ffmpeg / pillow-heif / exiftool
                       ┌────────▼─────────┐
                       │  /mnt/media      │
                       │  /var/lib/…/derived │
                       └──────────────────┘
```

**핵심 원칙: 바이트는 Python을 거치지 않는다.**
FastAPI는 권한 검사 후 `X-Accel-Redirect` 헤더만 돌려주고, 실제 파일 전송은 nginx가 한다.
Python이 4K 영상을 스트리밍하면 워커가 오래 점유되어 API 응답 전체가 밀린다.

### 3.1 포트 배정 **[확정]**

호스트 nginx가 80/443을 이미 점유하고 있고, 다른 서비스들도 각자 포트를 쓰고 있다.
실제 점유 현황은 `docs/environment.local.md` 참조.

| 서비스 | 포트 | 노출 범위 |
|---|---|---|
| FastAPI | **8005** | `127.0.0.1`만 (nginx가 프록시) |
| PostgreSQL | 5432 | **Docker 내부 네트워크 전용 — 호스트에 노출하지 않음** |
| Redis | 6379 | **Docker 내부 네트워크 전용** |

5432·6379는 현재 비어 있지만 **`ports:` 매핑을 아예 걸지 않는다.** 외부에서 접근이
불가능해져 보안이 올라가고, 나중에 다른 서비스와 포트가 겹칠 일도 없다.

### 3.2 nginx 설정 스케치 **[제안]**

서브도메인 방식을 권한다(예: `poogiegram.<도메인>`). 경로 방식(`/photos/`)은 SPA 라우터에
base path를 물려야 하고 쿠키 스코프도 지저분해진다.

```nginx
server {
    listen 443 ssl;
    server_name poogiegram.example.com;
    # ssl_certificate — certbot이 관리

    client_max_body_size 64m;          # 청크 업로드 단위 기준

    location / {
        proxy_pass http://127.0.0.1:8005;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # FastAPI가 X-Accel-Redirect 로 지시할 때만 도달 가능한 내부 경로
    location /_media/ {
        internal;
        alias /mnt/media/;
    }
    location /_derived/ {
        internal;
        alias /var/lib/poogiegram/derived/;
    }
}
```

`internal` 지시어가 핵심이다 — **브라우저가 이 경로로 직접 요청하면 404**가 되고,
FastAPI가 헤더로 지시했을 때만 서빙된다. 권한 검사를 우회할 수 없다.

### 3.3 파일 읽기 권한

nginx는 `www-data`로 동작하므로 `/mnt/media`와 `derived/`를 읽을 수 있어야 한다.
파일을 world-readable로 두는 대신 **`www-data`를 `poogiegram` 그룹에 넣고 `640`/`750`** 으로 간다.
M1 권한 구성(§6.1)에서 워커·SFTP 계정과 함께 처리한다.

### 3.4 구성 요소별 역할

| 컴포넌트 | 위치 | 역할 |
|---|---|---|
| nginx | **호스트 (기존)** | TLS, 기존 사이트, `/api` 프록시, `X-Accel-Redirect` 파일 서빙 |
| FastAPI | 컨테이너 :8005 | REST API, 인증, 권한, 검색, 업로드 수신 |
| PostgreSQL 17 | 컨테이너 (내부) | 미디어 메타데이터, 앨범/태그, 사용자, 작업 이력 |
| Redis | 컨테이너 (내부) | 작업 큐 + 세션 저장 |
| arq worker | 컨테이너 (내부) | 인제스트 파이프라인. ffmpeg·VA-API 호출 |
| React SPA | 빌드 산출물 | FastAPI가 정적 서빙하거나 nginx가 직접 서빙 |

> **[열림] Q17.** 어떤 도메인을 쓰시나요? 서브도메인을 하나 쓸 수 있으면
> `poogiegram.<도메인>` 형태로 잡겠습니다. certbot 인증서 발급도 그 시점에 함께 합니다.

---

## 4. 스토리지 설계 **[제안]** — 가장 중요한 결정

원본을 잃으면 끝이므로, 여기가 제일 보수적으로 가야 하는 지점.

### 4.0 물리 배치 **[확정]** — 2계층

**각 계층을 그 디스크가 잘하는 일에 맞춘다.**

| 디스크 | 용량 | 역할 | 이유 |
|---|---|---|---|
| 내장 NVMe | 수백 GB 여유 | **`derived/`** — 썸네일·프리뷰·HLS | 그리드 스크롤 = 작은 파일 수백 개 랜덤 읽기. **HDD가 가장 못하는 패턴** |
| 외장 HDD (대용량) | 1.8TB | **`/mnt/media`** — 원본·드롭폴더·휴지통·DB 덤프 | 순차 읽기 위주라 HDD로 충분. 용량이 핵심 |
| 외장 HDD (소용량) | 453GB | **미할당** | 로컬 백업 계획 철회 (§4.2). 다른 용도로 전용 |

**핵심은 파생물을 내장 SSD에 두는 것이다.** 타임라인을 스크롤하면 썸네일 수백 개를
동시에 요청한다. 이걸 외장 HDD에서 읽으면 헤드 탐색 때문에 눈에 띄게 버벅인다.
반대로 원본은 라이트박스에서 한 번에 한 장씩 순차로 읽으므로 HDD로 충분하다.

용량은 여유롭다 — `derived/` 추정 82GB가 내장 여유 332GB에 넉넉히 들어가고,
`originals/` 310GB는 1.8TB에서 수년치가 남는다.

### 4.1 디렉터리 레이아웃

```
/mnt/media/                     ← 외장 HDD. 이 디렉터리 하나가 "복제할 것"의 전부
│                                 root:root 755
├── originals/                  ← 앱이 내용을 절대 변경하지 않는 영역 (위치는 옮길 수 있음 §6.7)
│   ├── 2026/08/03/               ※ SFTP 사용자에게 보이지 않음 (chroot 밖)
│   │   ├── IMG_4821__a3f9c1d2.heic     # 원본명 유지 + 해시 앞 8자
│   │   └── IMG_4821__a3f9c1d2.mov      # 라이브 포토 동반 파일 (같은 스템 공유)
│   └── _undated/               ← 촬영일 판정 실패분. 날짜 확정 시 위 경로로 이동 (§6.7)
│
├── incoming/                   ← ★ SFTP ChrootDirectory. root:root 755 (쓰기 불가)
│   ├── drop/                   ← 사용자가 파일을 넣는 유일한 곳. poogiegram:2775
│   └── failed/                 ← 인제스트 실패분 + 사유. 사용자가 읽고 고칠 수 있게 노출
│
├── .tmp/                       ← 웹 업로드 청크 조립용. chroot 밖 (앱만 사용)
├── db/                         ← pg_dump 결과 (§4.2)
└── trash/                      ← 삭제 시 30일 보관 후 실제 삭제

/var/lib/poogiegram/derived/    ← 내장 NVMe. 재생성 가능 → 복제 대상 아님
└── a3/f9/a3f9c1d2.../
    ├── thumb_320.webp
    ├── preview_1600.webp
    ├── display.jpg             # HEIC 원본의 브라우저 표시용 (필수)
    └── hls/{master.m3u8, 720p_000.ts, ...}
```

`incoming/`과 `trash/`를 `originals/`와 같은 디스크에 둔 것은 의도적이다.
**같은 파일시스템 안에서는 이동이 `rename()` 한 번**이라 즉시 끝난다.
디스크가 다르면 실제 복사가 일어나 대용량 파일에서 느려지고, 도중에 죽으면 반쪽 파일이 남는다.

**왜 날짜 경로 + 해시 접미사인가**
- 순수 해시 경로(CAS)는 파일탐색기로 열었을 때 사람이 아무것도 알아볼 수 없다.
  앱이 죽어도 파일만으로 복구 가능해야 한다.
- 날짜만 쓰면 동일 파일명 충돌이 난다. 해시 8자를 붙여 해결.
- 날짜 판정은 단순히 "EXIF 없으면 mtime"이 아니다 — **§6.7의 우선순위 체인**을 따른다.

**"원본은 건드리지 않는다"의 정확한 의미**
- 앱은 원본 파일의 **내용을 절대 변경하지 않는다.** 이건 예외 없다.
- 다만 **위치는 옮길 수 있다** — 촬영일이 나중에 확정되면 `_undated/`에서 날짜 경로로
  이동한다(§6.7). 같은 파일시스템 안이라 `rename()` 한 번이고, 애초에 날짜 경로를 쓰는
  이유(파일탐색기로도 알아볼 수 있게)가 날짜가 맞아야 성립한다.
- 이동 시 `asset.path`를 함께 갱신한다. 앱 외의 프로세스는 여전히 이 영역을 건드리지 않는다.

**중복 제거**: 인제스트 시 SHA-256 전체 해시 계산 → 이미 있으면 파일 저장 생략하고
DB에 참조만 추가. 사진은 여기저기서 여러 번 들어오므로 실질적 효과가 크다.

### 4.2 복제 경계 **[확정]** — 로컬 백업은 만들지 않는다

**`/mnt/media` 하나만 복제하면 전부 복구된다.** `derived/`는 지워도 재생성되므로
복제 대상이 아니다. **이 경계를 흐리지 않는 것이 설계의 핵심**이고, 이 원칙 자체는
어떤 백업 수단을 고르든 그대로 유효하다.

DB 덤프를 `/mnt/media/db/`에 두는 이유가 여기 있다 — **복제 대상이 디렉터리 하나로 끝난다.**
원본과 메타데이터가 항상 함께 움직인다.

```bash
pg_dump -Fc poogiegram > /mnt/media/db/$(date +%F).dump
```

#### 로컬 백업 디스크 계획은 철회한다

보조 외장 디스크에 `originals/`를 rsync하는 구성을 검토했으나 **채택하지 않는다.**

- 같은 기계에 붙은 디스크는 화재·도난·전원 서지·실수로 인한 `rm -rf`를 막지 못한다
- 원본을 별도로 복제해 둘 계획이 있으므로 중복 투자다
- 500GB가 오히려 **라이브러리 상한을 400GB로 묶는 제약**이 됐다

→ 보조 외장 디스크는 **미할당**으로 두고 다른 용도로 쓴다.
→ **용량 상한이 400GB에서 1.8TB로 풀린다.** §4.4 추정 310GB 기준으로 수년치 여유다.

#### 남는 위험 — 인지하고 간다

**현재 백업이 없다.** 로컬 백업도 원본이 사는 디스크가 죽는 상황은 막아줬는데,
그 구간이 비어 있다. 사진은 재취득이 불가능한 데이터다.

실제 복제본이 마련되기 전까지는 이 상태라는 점을 인지하고 간다.
수단은 열려 있다 — 주기적으로 뽑아두는 외장, 다른 기계로의 rsync, 클라우드 동기화 등
무엇이든 **`/mnt/media` 하나만 복사하면 된다**는 점은 동일하다. §11 Q8.

### 4.3 백지 시작의 이점 **[확정]**

기존 라이브러리가 없으므로 **앱이 모든 파일을 처음부터 소유**한다. 그 결과:
- 외부 경로를 참조하는 "제자리 인덱싱" 모드가 불필요 → `asset.path`는 항상 `originals/` 기준 상대경로 하나로 통일.
- 파일이 앱 밖에서 이동·삭제되는 상황을 방어할 필요가 없다 → 경로 재동기화 로직 없음.
- 명명 규칙·디렉터리 구조를 **1일차부터 강제**할 수 있다. 나중에 뒤집을 일이 없다.

`originals/`는 앱 외의 프로세스가 건드리지 않는다는 것을 운영 규칙으로 못박는다.

### 4.4 용량 추정 **[제안]** — 아이폰·스냅 기준 재산정

| 소스 | 장당 크기 | 비고 |
|---|---|---|
| 아이폰 HEIF 24MP (기본) | 2~3 MB | 대다수가 여기 해당 |
| 아이폰 HEIF Max 48MP | 5~8 MB | 설정에서 켠 경우 |
| 아이폰 ProRAW 48MP | ~75 MB | 켜져 있으면 용량을 크게 잡아먹는다 |
| **스냅사진 JPEG 원본** | 20~40 MB | 풀프레임 고화소 납품본 — **JPEG만 받음 [확정]** |
| ~~스냅사진 RAW~~ | ~~50~80 MB~~ | **범위 제외** — RAW 현상 파이프라인 불필요 |
| 아이폰 4K60 HEVC 영상 | ~400 MB / 분 | |
| 라이브 포토 동반 MOV | 1~3 MB | **살리기로 확정** — 사진 장수만큼 붙는다 |

**시나리오: 사진 30,000장(평균 4MB) + 스냅 2,000장(평균 25MB) + 영상 200분**
```
originals   120 GB + 50 GB + 80 GB   ≈ 250 GB
derived      thumb 320    30k × 25KB  ≈  0.8 GB
             preview 1600 30k × 300KB ≈  9 GB
             display.jpg  30k × 800KB ≈ 24 GB   ← HEIC 때문에 새로 생기는 비용
             HLS          영상분량의 약 0.6배 ≈ 48 GB
                                       ─────────
                             derived 합계 ≈ 82 GB
```
→ 라이브 포토 MOV(3만 × 2MB ≈ 60GB)를 더하면 **총 390GB 내외.**
2TB 디스크면 여유롭고, 4TB면 수년치가 들어간다.

주목할 점: `derived/`가 원본의 **약 33%**까지 커진다. HEIC 표시용 사본이 큰 몫이다.
그래도 §4.2 백업 경계 덕에 **백업 대상은 `originals/` 310GB뿐**이다.
(라이브 포토 MOV는 원본이므로 백업 대상에 포함된다.)

### 4.5 RAW 범위 제외 **[확정]**

스냅사진은 **JPEG 납품본만** 받는 것으로 확정. 그 결과 통째로 빠지는 것들:
- `rawpy` / `darktable-cli` 기반 현상 단계
- 현상 파라미터 저장·재현상 로직
- RAW 포맷별 분기 (DNG / CR3 / ARW / NEF …)
- 장당 50~80MB에 따른 용량·전송 부담

**M1이 눈에 띄게 가벼워진다.** 이미지 입력 포맷이 HEIC와 JPEG 둘뿐이라 인제스트 분기가 단순하다.
(아이폰 ProRAW를 쓰게 되면 그때 다시 검토한다 — 지금 미리 만들지 않는다.)

### 4.6 외장 디스크 운영 규칙 **[제안]**

원본이 외장에 올라가므로, 여기가 무너지면 전부 무너진다.

#### 파일시스템 **[확정]** — 두 디스크 모두 이미 ext4

재포맷이 필요 없다. exFAT/NTFS/HFS+였다면 저널링 부재나 리눅스 쓰기 불안정 때문에
재포맷이 불가피했겠지만, 이미 원본 보관에 적합한 상태다.

**구성 완료 [확정]** — 2026-08-07 적용됨.

| 장치 | 마운트 | 실용량 | UUID |
|---|---|---|---|
| 외장 HDD (대용량) | `/mnt/media` | 원본·드롭·DB덤프 |
| 외장 HDD (소용량) | `/mnt/backup` | 미할당 (§4.2) |

- 둘 다 ext4, `tune2fs -m 1`로 예약 블록 회수 완료 (백업 디스크 434G → **453G**)
- fstab에 UUID + `nofail` + `x-systemd.device-timeout=30s`로 등록, `findmnt --verify` 통과

**`sdb1` 기존 데이터는 폐기 확정 [확정]** — 약 200GB가 들어 있었으나 전량 불용으로 확인됐다.
- `downloads` 183GB — 사용하지 않음
- Docker 데이터 루트 약 14GB — **예전 다른 환경의 잔여물.** 현재 서버의 Docker는
  내장 SSD(`/var/lib/docker`)에서 정상 동작 중이며 이것과 무관하다

→ `mkfs.ext4`로 전체 초기화 후 미디어 전용 디스크로 사용한다. 2TB 전체를 쓸 수 있다.

#### 예약 블록 회수 **[제안]**

ext4는 기본적으로 용량의 **5%를 root 전용으로 예약**한다. 루트 파티션에서는 디스크가 가득 차도
시스템이 죽지 않게 하는 안전장치지만, **데이터 전용 디스크에서는 그냥 낭비다.**

```bash
sudo tune2fs -m 1 /dev/sda1     # +약 23GB
sudo tune2fs -m 1 /dev/sdb1     # +약 92GB
```

비파괴 작업이며 즉시 반영된다. `sdb`의 92GB가 그대로 원본 보관 용량이 된다.

#### fstab에는 반드시 UUID와 `nofail`

현재 `/mnt/sda1`, `/mnt/sdb1`에 마운트돼 있다. **역할이 드러나는 이름으로 바꾸길 권한다** —
`sda`/`sdb`는 사람이 보기에 어느 쪽이 원본이고 어느 쪽이 백업인지 알 수 없어,
운영 중 실수를 부른다.

```
UUID=<미디어 디스크 UUID>   /mnt/media   ext4  defaults,nofail,x-systemd.device-timeout=30s  0 2
UUID=<보조 디스크 UUID>     /mnt/backup  ext4  defaults,nofail,x-systemd.device-timeout=30s  0 2
```

USB 장치명(`/dev/sdb`)은 **연결 순서에 따라 바뀐다.** 재부팅 후 sda와 sdb가 뒤바뀐 상태에서
장치명으로 마운트하면 엉뚱한 디스크에 원본을 쓰는 사고가 난다. 위처럼 UUID로 고정한다.
`sda1`(`/mnt/backup`)은 현재 미할당이지만, 용도가 정해질 때까지 마운트는 유지한다.

**현재 마운트가 fstab에 등록돼 있는지 확인이 필요하다.** 수동 마운트 상태라면 재부팅 시 사라진다.
```bash
grep -E 'sda|sdb|media|backup' /etc/fstab
```

**`nofail`이 없으면 디스크가 빠졌을 때 부팅이 멈춘다.** 디스플레이 없는 헤드리스 서버에서는
치명적이다 — 화면을 볼 수 없으니 왜 안 켜지는지도 알 수 없다.

#### 마운트 확인 후 기동

`nofail` 때문에 디스크가 없어도 부팅은 되지만, 그러면 앱이 **빈 마운트 포인트에 새 파일을 쓴다.**
원본이 루트 파티션에 쌓이고 라이브러리는 텅 빈 것처럼 보인다.

→ 앱 시작 시 `/mnt/media/originals/.poogiegram-ok` 마커 파일 존재를 확인하고,
없으면 **기동을 거부**한다. Docker Compose에서도 헬스체크로 건다.

#### SMART 모니터링

원본이 사는 디스크이므로 상시 감시한다. 이미 한 번 수동으로 테스트하셨으니 자동화만 하면 된다.

```bash
sudo apt install smartmontools
# /etc/smartd.conf 에 주 1회 short + 월 1회 long 테스트 + 메일 알림
```

특히 **Reallocated_Sector_Ct**와 **Current_Pending_Sector**가 0에서 올라가기 시작하면
디스크 교체 신호다. §11 Q7과 연결된다.

---

## 5. 데이터 모델 **[제안]**

```sql
-- 물리 파일 1개 = asset 1개 (해시로 유일)
asset(
  id            uuid pk,
  sha256        text not null,          -- 중복 판별. UNIQUE 인덱스는 아래 참조
  path          text not null,          -- originals/ 기준 상대경로 (해시 접미사 포함)
  original_filename text not null,      -- 사용자가 올린 그대로의 이름. 파일명 검색용
  kind          text,                   -- 'image' | 'video'
  mime          text,
  bytes         bigint,
  width, height int,
  duration_ms   int,                    -- video

  taken_local   timestamp NOT NULL,     -- 촬영 시각(현지, tz 없음). 판정 체인은 §6.7
  taken_at      timestamptz NOT NULL,   -- 절대 시각. 타임라인 정렬 기준
  tz_offset     int,                    -- 촬영지 오프셋(분). 불명이면 NULL
  date_source   text NOT NULL,          -- 날짜를 어디서 얻었는지 (§6.7)
                                        -- 'exif'|'xmp'|'filename'|'sibling'|'mtime'|'manual'|'unknown'
  taken_md      smallint GENERATED ALWAYS AS (
                    EXTRACT(MONTH FROM taken_local)::int * 100
                  + EXTRACT(DAY   FROM taken_local)::int
                ) STORED,               -- 현지 기준 월일(0807). "과거의 오늘" 인덱스용

  lat, lon      double precision,       -- 위경도. point 대신 분리 (좌표 순서 혼동 방지)
  place         text,                   -- 역지오코딩 결과 (§5.4 참조, 지연 채움)
  camera        text,
  exif          jsonb,                  -- 원본 EXIF 통째로 (스키마 변경 없이 확장)

  -- 아이폰 대응으로 추가된 컬럼 --------------------------------
  codec         text,                   -- 'hevc' | 'h264' | 'heic' | 'jpeg' | 'dng' ...
  is_hdr        bool,                   -- Dolby Vision / HLG 여부 → 톤매핑 대상
  needs_display_copy bool,              -- HEIC 등 브라우저 미지원 → display.jpg 필수
  derive_status text,                   -- 'pending'|'ready'|'failed'  (사진·영상 공통)
  video_status  text,                   -- 'direct' | 'needs_transcode' | 'ready' | 'failed'
  content_id    text,                   -- Apple ContentIdentifier. 라이브 포토 페어링 (§6.5)
  live_motion_id uuid references asset, -- 라이브 포토의 동반 MOV (정지컷 쪽에만 채움)
  is_live_motion bool default false,    -- 동반 MOV 본인 → 타임라인에서 숨김
  -- ----------------------------------------------------------

  -- 큐레이션 · 추억 기능 (§5.1) ---------------------------------
  is_favorite   bool default false,     -- 사용자가 명시적으로 표시
  is_screenshot bool default false,     -- 카메라 메타데이터 부재로 판별. 추억에서 제외
  -- ----------------------------------------------------------

  -- 사용자 편집 (§5.3) -----------------------------------------
  rotation      smallint default 0,     -- 0|90|180|270. 원본은 불변이므로 파생물에 적용
  caption       text,                   -- 사용자 메모. 검색 대상
  -- ----------------------------------------------------------

  owner_id      uuid references app_user,  -- 업로드한 사람. 출처 기록용 (§5.2 — 권한 아님)
  created_at    timestamptz not null default now(),
  updated_at    timestamptz not null default now(),
  deleted_at    timestamptz               -- soft delete → trash/ 30일 보관
)

album(id, name, cover_asset_id, owner_id, created_at, updated_at)
album_asset(album_id, asset_id, position,  primary key (album_id, asset_id))
tag(id, name text unique)
asset_tag(asset_id, tag_id,  primary key (asset_id, tag_id))
app_user(id, username unique, password_hash, display_name, role)  -- role: admin | member

-- 앨범 또는 자산 중 정확히 하나만 가리킨다
share_link(
  id, token text unique, expires_at, password_hash, allow_download bool,
  album_id uuid references album,
  asset_id uuid references asset,
  check (num_nonnulls(album_id, asset_id) = 1)
)

-- 큐가 아니라 이력 로그 (§5.2)
ingest_log(id, asset_id, source_filename, event, error, created_at)
```

**인덱스**
```sql
CREATE INDEX ON asset (taken_at DESC)   WHERE deleted_at IS NULL;            -- 타임라인
CREATE INDEX ON asset (taken_md, taken_local DESC) WHERE deleted_at IS NULL; -- 과거의 오늘
CREATE UNIQUE INDEX ON asset (sha256);                                       -- 중복 제거 (§5.2)
CREATE INDEX ON asset (content_id)      WHERE content_id IS NOT NULL;        -- 라이브 포토 페어링
CREATE INDEX ON asset USING GIN (exif);                                      -- EXIF 임의 질의
```
검색용 `tsvector`는 §9(검색)에서 별도로 정의한다.

**타임라인 쿼리는 항상 `where is_live_motion = false`** — 동반 MOV가 목록에 새지 않게.

**설계 의도**
- EXIF를 `jsonb` 통째로 보관 → 나중에 "이 렌즈로 찍은 것만" 같은 요구가 와도 마이그레이션 불필요.
- **`taken_local`을 별도 컬럼으로 두는 이유** — 두 가지 문제를 동시에 푼다.
  1. **현지 날짜가 맞아야 한다.** KST는 UTC+9이므로 UTC = 현지 − 9시간이다.
     서울에서 8월 8일 **오전** 8시 30분에 찍은 사진은 UTC로는 8월 7일 23시 30분,
     즉 **전날**이다. UTC 기준으로 날짜를 자르면 아침에 찍은 사진이 하루 전으로 밀려
     "과거의 오늘"이나 날짜 헤더가 어긋난다.
  2. **`timestamptz`로는 월/일 인덱스를 만들 수 없다.** Postgres에서
     `EXTRACT(MONTH FROM timestamptz)`는 세션 타임존에 의존해 **IMMUTABLE이 아니라 STABLE**이다.
     생성 컬럼이나 표현식 인덱스에 쓸 수 없어 전체 스캔이 된다. `timestamp`(tz 없음)에
     대한 `EXTRACT`는 IMMUTABLE이므로 `taken_md`를 생성 컬럼으로 만들 수 있다.
- `taken_at`(절대 시각)도 함께 유지한다 — 서로 다른 타임존에서 찍힌 사진들의
  정확한 선후 관계는 이쪽으로만 판정할 수 있다.
- **`original_filename`을 따로 둔다** — `path`에는 해시 접미사가 붙은 이름이 들어간다.
  사용자가 검색창에 치는 건 `IMG_4821.HEIC`이지 `IMG_4821__a3f9c1d2.heic`가 아니다.
- **`lat`/`lon`을 `point` 대신 분리** — `point(x, y)`는 (경도, 위도) 순서인데 사람은
  보통 (위도, 경도)로 생각해서 뒤집어 넣는 실수가 잦다. 컬럼을 나누면 그 여지가 없고,
  반경 검색이 필요해지면 `earthdistance` 확장을 얹으면 된다.
- `derive_status`를 **사진에도** 둔 이유: HEIC는 파생물이 생기기 전까지 크롬에서 아예 안 보인다.
  "원본은 있는데 화면엔 안 뜨는" 상태를 UI가 구분해서 표시해야 한다.
  영상은 `derive_status`(썸네일)와 `video_status`(HLS)를 **둘 다** 갖는다.
- 라이브 포토를 **양방향이 아닌 단방향 참조**로: 정지컷이 주(主), MOV가 종(從).
  삭제·공유·앨범 편입은 전부 정지컷 기준으로 동작하고 MOV는 따라간다.

### 5.1 "과거의 오늘" **[스키마는 지금, 구현은 v2]**

`taken_at`이 이미 설계의 주축이라 **새 테이블 없이 같은 축을 다르게 자르면 된다.**
다만 위 세 컬럼(`taken_local` / `taken_md` / `is_favorite`)과 `is_screenshot`은
**지금 넣는다** — 나중에 추가하면 수만 행 백필이 필요하지만, 지금은 거의 공짜다.

기본 질의:
```sql
SELECT * FROM asset
WHERE taken_md = 807                           -- 8월 7일 (현지 기준)
  AND taken_local < date_trunc('year', now())  -- 올해 제외
  AND deleted_at IS NULL
  AND is_live_motion = false
  AND is_screenshot = false
  AND date_source NOT IN ('mtime', 'unknown')  -- 촬영일이 불확실한 것 제외 (§6.7)
ORDER BY taken_local DESC;
```

#### 진짜 어려운 건 쿼리가 아니라 큐레이션

어떤 날은 사진이 200장이고 어떤 날은 0장이다. **200장을 그대로 던지면 "추억"이 아니라
그냥 목록이다.** 가진 신호로 걸러낸다.

| 규칙 | 근거 |
|---|---|
| **앨범에 속한 사진 우선** | 앨범에 넣었다는 것 자체가 사람이 이미 고른 결과 |
| `is_favorite` 우선 | 명시적 선호 |
| 연도별 그룹 + 연도당 상한 N장 | 한 해가 화면을 독점하지 않게 |
| 총 개수가 임계치 미만이면 그날은 표시하지 않음 | 2~3장짜리 "추억"은 오히려 초라하다 |
| **`is_screenshot` 제외** | 아래 참조 |

**스크린샷 제외가 체감상 가장 큰 차이를 만든다.** 아이폰 스크린샷은 EXIF에 카메라
기종·렌즈·노출 정보가 없어서 인제스트 시점에 거의 정확히 판별된다. 이걸 안 거르면
추억 화면에 카톡 캡처와 영수증이 섞인다.

#### 같은 구조로 확장 가능한 것들

| 종류 | 필요한 것 | 상태 |
|---|---|---|
| 이 장소에서 | `gps` / `place` | **이미 있음** |
| N년 전 여행 | 시간·장소 간격 클러스터링 | 추가 코드만 |
| 특정 인물 | 얼굴 인식 | v2 이후 |

### 5.2 정책 **[확정]**

스키마만으로는 드러나지 않지만 구현이 갈리는 결정들.

#### 접근 범위 — 모든 구성원이 전부 본다

로그인한 구성원은 **모든 사진을 본다.** `owner_id`는 "누가 올렸는지" 출처 기록일 뿐
**권한 제어에 쓰지 않는다.** 가족 사진첩이라는 성격에 맞고, 모든 조회 쿼리에서
권한 필터가 사라져 구조가 단순해진다.

- 삭제는 누구나 가능하되 **휴지통 30일 보관이 안전망** 역할을 한다
- 휴지통 영구 삭제는 `admin`만
- 나중에 비공개가 필요해지면 큰 변경이 된다는 점은 인지하고 간다 (모든 쿼리에 필터 추가)

#### 중복 업로드 — 소프트 삭제된 자산은 되살린다

`sha256`에 UNIQUE 제약이 있으므로, **삭제한 사진을 다시 올리면 INSERT가 실패한다.**
휴지통 30일 안에 흔히 일어나는 상황이다.

→ 인제스트가 해시 충돌을 만나면 기존 행을 확인한다.
- `deleted_at IS NULL` → 진짜 중복. 파일 삭제하고 "이미 있음"으로 기록
- `deleted_at IS NOT NULL` → **부활**. `deleted_at`을 비우고 `trash/`의 파일을
  `originals/`로 되돌린다. 새 행을 만들면 같은 사진이 둘이 된다

#### `ingest_log`는 큐가 아니다

작업 큐는 **Redis + arq**가 들고 있다. DB에 큐 상태를 중복 저장하면 둘이 어긋나고,
어느 쪽이 진실인지 알 수 없어진다.

→ `ingest_log`는 **append-only 이력**으로만 쓴다(무엇이 언제 어떻게 됐는지, 실패 사유).
§6.1의 진행 상황 표시(`대기·처리중·완료·실패`)는 이렇게 집계한다.

| 표시 | 출처 |
|---|---|
| 대기 | `incoming/`의 파일 수 (파일시스템) |
| 처리 중 | arq 활성 작업 수 |
| 완료 | `asset.derive_status = 'ready'` |
| 실패 | `incoming/failed/` 파일 수 + `derive_status = 'failed'` |

### 5.3 UI 편집 기능이 스키마에 요구하는 것 **[일부 구현]**

대부분의 편집 기능은 나중에 붙여도 스키마에 영향이 없다. **컬럼이 필요한 것만 지금 넣는다** —
나중에 추가하면 수만 행 백필이 필요하기 때문이다.

#### 이미 커버된 것

| 기능 | 근거 |
|---|---|
| 삭제 / 복원 | soft delete + `trash/` 30일 (§5.2, 아래 경로 규약) |
| 촬영일 변경 | `date_source='manual'` + 파일 이동 (§6.7) |
| **촬영일 변경 되돌리기** | `exif` jsonb에 원본이 통째로 남아 원래 값 복구 가능 |
| 즐겨찾기 | `is_favorite` |
| 앨범 편입·수동 정렬 | `album_asset.position` |
| 태그 | `asset_tag` |
| 라이브 포토 동반 삭제 | 단방향 참조 — 정지컷이 주(主) (§6.5) |

#### 지금 넣는 컬럼

**`rotation`** — 원본 EXIF를 고칠 수 없으므로(내용 불변 원칙, §4.1) **회전값을 DB에 들고
파생물 생성 시 적용**한다. 아이폰 사진은 EXIF Orientation이 정확해 쓸 일이 적지만,
스캔본이나 스냅사진 내보내기에서 어긋나는 경우가 있다.
회전을 바꾸면 해당 자산의 파생물을 재생성한다(`derive_status='pending'`으로 되돌림).
**원본을 다운로드하면 회전 전 상태로 나온다** — 원본 불변의 당연한 귀결이며 일반적인 동작이다.

**`caption`** — 검색 대상이므로 §9의 `tsvector` 설계에 포함되어야 한다.
나중에 추가하면 검색 인덱스를 다시 만들어야 한다.

#### 휴지통 경로 규약 **[확정]**

소프트 삭제 시 **`asset.path`는 바꾸지 않고 루트만 다르게 해석한다.**

| `deleted_at` | 실제 파일 위치 |
|---|---|
| `NULL` | `/mnt/media/originals/` + `path` |
| 값 있음 | `/mnt/media/trash/` + `path` |

삭제는 `originals/`→`trash/`, 복원은 그 반대로 **같은 상대경로를 유지한 채 이동**한다.
경로 문자열을 건드릴 일이 없고, §5.2의 "부활"(삭제된 자산 재업로드)도 역방향 이동 한 번이다.

#### 태그는 입력 마찰을 줄이는 것이 설계 목표다 **[요구사항]**

수동 태깅은 **비용이 사진 한 장당 발생하고 효용은 나중에 온다.** 다른 서비스에서
태그가 방치되는 것도 기능이 나빠서가 아니라 입력이 번거롭기 때문이다.
따라서 태그 UI는 "기능이 있다"가 아니라 **"몇 초 만에 끝난다"를 기준으로 만든다.**

| 요구사항 | 이유 |
|---|---|
| **일괄 태깅** — 여러 장 선택 후 한 번에 부여/해제 | 한 번의 동작이 200장을 커버해야 실제로 쓰인다 |
| **자동완성** — 기존 태그에서 제안 | 없으면 `여행`/`여행지`/`trip`으로 표기가 흩어져 검색이 무의미해진다 |
| **최근·자주 쓴 태그 우선 노출** | 실제로는 소수의 태그를 반복해서 쓴다 |
| **라이트박스 키보드 단축키** | 한 장씩 훑으며 태깅할 때 마우스 왕복이 가장 큰 마찰 |
| **태그 병합·이름 변경** | 표기가 흔들린 뒤에도 되돌릴 수 있어야 한다 |

태그는 §5.2의 접근 모델에 따라 **전 구성원 공용**이다(`tag.name`이 전역 unique).
가족이 같은 어휘를 공유하는 편이 검색에 유리하다.

#### 구현 상태

| 항목 | 상태 |
|---|---|
| 일괄 태그 부여·해제 | ✅ `/api/edit/tags` — 선택 모드 + 자동완성 |
| 소프트 삭제 / 복원 | ✅ `/api/edit/delete`, `/restore` — 위 경로 규약대로 |
| 태그 이름 변경·병합 | ✅ `PATCH /api/tags/{id}` — **이미 있는 이름으로 바꾸면 병합된다.** "합치기"를 따로 두면 사용자가 두 기능의 차이를 먼저 이해해야 한다 |
| 태그 삭제 | ✅ `DELETE /api/tags/{id}` — 이름표만 없애고 사진은 남긴다 |
| 라이브 포토 동반 삭제 | ✅ 정지컷을 지우면 동반 MOV 도 함께 간다 — 안 그러면 타임라인에 안 보이는 파일이 디스크에 남는다 |
| 휴지통 화면 (복원 UI) | 미구현 — API 는 있다 |
| 30일 자동 비우기 | 미구현 |
| 촬영일 변경, 회전, 즐겨찾기 | 미구현 (스키마는 준비됨) |
| 사진별 태그 보기·붙이기·떼기 | ✅ 라이트박스 하단. 이게 없으면 **사진에 뭐가 붙어 있는지 볼 방법이 없어** 태그를 하나씩 눌러 확인해야 했다 |
| 라이트박스 키보드 단축키 | 미구현 (마우스로는 가능) |

**일괄 요청에 상한(500건)을 둔다.** 무제한이면 실수로 전체를 지우는 요청이 그대로 통과한다.

**빈 태그를 자동으로 지우지 않는다.** 태그의 마지막 사진을 지우면 그 사진은 휴지통으로 가고
개수는 0이 되지만 `asset_tag` 연결은 살아 있다. 여기서 태그를 자동 삭제하면 `CASCADE`로 연결까지
사라져 **사진을 복원해도 태그가 돌아오지 않는다.** 대신 목록에 개수를 함께 보여주고 지울 수단을 준다.

### 5.4 역지오코딩은 좌표를 먼저 묶는다 **[제안]**

`place`를 채우려면 외부 지오코딩 서비스를 호출해야 하는데, **사진 한 장당 한 번씩
호출하면 현실적으로 불가능하다.** Nominatim은 초당 1건 정책이라 3만 장이면 8시간이 넘는다.

→ **GPS 좌표를 먼저 클러스터링하고 대표점만 조회한다.** 같은 여행·같은 장소의 사진은
좌표가 거의 같으므로, 3만 건이 수백 건으로 줄어든다.

```
좌표를 소수점 3자리(약 100m)로 반올림 → 고유 좌표 집합 추출
  → 각 대표점 1건씩 지오코딩 (초당 1건 준수)
  → 결과를 place_cache 에 저장하고 asset.place 에 전파
```

`place`는 **없어도 서비스가 동작하는 부가 정보**다. 지연 채움으로 두고,
실패해도 인제스트 자체를 막지 않는다.

---

### 5.5 태그 — 컨셉별로 모아 보기 **[확정]**

"2025년 결혼기념일", "푸기 3번째 생일"처럼 **사건 단위로 묶어서 보고 싶다**는 요구가 실제
사용자(가족)에게서 나왔다. 날짜순 타임라인만으로는 특정 사건을 찾을 수 없다.

**앨범이 아니라 태그를 쓴다.** 한 사진이 여러 묶음에 들 수 있고(가족 여행이면서 생일),
순서·표지 개념이 필요 없어 폴더에서 자동 생성하기에 맞는다. 공유가 필요해지면 태그로
걸러 앨범을 만들면 된다 — `share_link`가 앨범을 가리키므로 그때 경로가 열린다.

#### 붙이는 방법이 핵심이다

한 장씩 지정하는 UI만 있으면 **아무도 쓰지 않는다.** 수백 장을 손으로 태깅할 사람은 없다.
그래서 넣는 시점에 자동으로 붙어야 한다.

**드롭 폴더의 하위 폴더 이름이 태그가 된다.** 스캐너는 이미 하위 폴더를 재귀 탐색하는데
(§6.1), 배치 기준이 EXIF 촬영일이라 **폴더 이름을 버리고 있었다.** 이미 읽고 있는 정보를
쓰기만 하면 된다.

경로의 **각 단계가 각각 태그**가 된다:

```
drop/푸기 3번째 생일/IMG_1.heic   → [푸기 3번째 생일]
drop/2025/결혼기념일/IMG_2.heic   → [2025] [결혼기념일]
drop/IMG_3.heic                   → 태그 없음
```

평평하게 쓰면 "폴더 하나 = 태그 하나"로 떨어지고, 정리하고 싶으면 중첩하면 된다.
마지막 폴더만 쓰면 서로 다른 해의 "생일"이 한 태그로 합쳐진다. 경로를 통째로 한 태그로
만들면 `2025/결혼기념일`과 `2026/결혼기념일`이 남남이 되어 "결혼기념일" 전체를 못 본다.

#### 이름 정규화

- 앞뒤 공백 제거, 내부 연속 공백은 하나로 — `푸기  생일`과 `푸기 생일`이 갈라지는 것을 막는다
- 60자 상한. 폴더 이름은 길어질 수 있는데 UI 에서 감당이 안 된다
- 빈 이름·숨김 폴더·시스템 폴더는 태그로 만들지 않는다 (§6.1의 무시 규칙을 그대로 쓴다)

오타로 비슷한 태그가 생기는 것은 **막을 수 없다.** 이름 변경·병합 기능이 결국 필요하다.
지금은 넣지 않되, `tag.name`이 unique 이므로 변경은 UPDATE 한 번, 병합은 `asset_tag`
이동이라는 점만 확인해 둔다.

#### 태그가 많아질 때

가족 사진은 사건이 계속 늘어난다. 수백 개를 전제해야 한다.

- 헤더의 태그 버튼 → 검색창이 있는 패널. 목록은 **사진이 많은 순**이다. 자주 쓰는 것이
  위로 올라온다
- 선택하면 헤더에 칩으로 남고 ✕ 로 해제
- 목록 API 는 개수를 함께 준다. "빈 태그"가 쌓이는 것을 눈으로 확인할 수 있어야 한다

#### 인제스트 후 빈 폴더

파일이 `originals/`로 옮겨지면 드롭 폴더에 빈 디렉터리가 남는다. 치우지 않으면 SFTP 로
접속했을 때 지난 폴더가 계속 쌓여 다음 업로드에 방해가 된다. 인제스트 후 빈 디렉터리는
지운다 — 단 드롭 루트 자체는 남긴다.

## 6. 미디어 파이프라인 **[제안]**

### 6.1 드롭 폴더 인제스트 **[제안]**

**원칙: 사용자가 보는 경로는 `incoming/` 하나뿐이다.** 폴더 구조도, 파일명 규칙도,
날짜별 정리도 신경 쓰지 않는다. 그냥 던져 넣으면 앱이 EXIF를 읽어 알아서 배치한다.

#### 전체 흐름

```
사용자가 incoming/drop/ 에 파일 투입   (SFTP · 웹 업로드 — §6.1 하단)
   ↓
감지        주기 스캔(기본) + inotify(가속) + 수동 트리거
   ↓
안정성 검사  크기·mtime이 30초간 변하지 않아야 처리 대상 ← 가장 중요
   ↓
파이프라인   SHA-256 → EXIF 추출(exiftool) → 코덱·HDR·라이브포토 판정
           → 촬영일 판정 체인(§6.7) → 스크린샷 판별(§5.1)
   ↓
배치        originals/YYYY/MM/DD/ 로 rename → DB asset 행 생성 (derive_status='pending')
   ↓
[큐]        썸네일 320 / 프리뷰 1600
[큐]        HEIC면 display.jpg          ← 이게 끝나야 크롬에서 보인다
[큐]        동영상이면 코덱·HDR 검사 후 필요시 HLS
[큐]        라이브 포토면 정지컷과 MOV 페어링
   ↓
결과        성공 → incoming/drop/ 에서 사라짐 (originals로 이동했으므로)
           중복 → 파일 삭제 + "이미 있음"으로 기록 (오류 아님)
           실패 → incoming/failed/ 로 이동 + 사유 기록
```

#### 가장 중요한 문제: 업로드 중인 파일 **[필수]**

**드롭 폴더 설계에서 반드시 밟는 함정이다.** SFTP로 500MB 영상을 올리는 중에도
파일은 `incoming/`에 **즉시 보인다.** 크기가 0바이트에서 조금씩 자라는 중인데,
스캐너가 이때 집어가면 **잘린 파일이 원본으로 저장된다.** 게다가 원본은 앱이
수정하지 않는 영역이라, 나중에 발견해도 되돌리기 번거롭다.

세 겹으로 막는다.

| 방어 | 내용 |
|---|---|
| **안정성 검사** | 크기와 mtime이 **30초간 변하지 않아야** 처리 대상. 클라이언트 종류와 무관하게 동작하는 유일한 방법 |
| **임시 파일 패턴 무시** | `.`로 시작하는 파일, `*.part` / `*.filepart` / `*.crdownload` / `.DS_Store` 제외 |
| **웹 업로드는 원자적 rename** | 서버가 쓰기를 제어하므로 `.tmp/`에 조립 후 완료 시점에 `incoming/drop/`으로 rename. 중간 상태가 노출되지 않는다 |

30초는 시작값이다. 느린 네트워크에서 전송이 끊겼다 이어지면 오탐이 날 수 있으므로
`INGEST_STABLE_SECONDS`로 조절 가능하게 둔다.

#### 감지 방식 — 주기 스캔이 기준, inotify는 가속

| 방식 | 장점 | 한계 |
|---|---|---|
| **주기 스캔** (기본, 5분) | 놓치는 게 없다. 단순하고 예측 가능 | 최대 5분 지연 |
| **inotify** (가속) | 즉시 반응 | 워치 한도(`max_user_watches`) 초과나 이벤트 큐 오버플로 시 **조용히 놓친다** |
| **수동 트리거** | 사용자가 "지금 처리" 버튼/API로 즉시 실행 | — |

**inotify를 단독 신뢰하면 안 된다.** 수백 장을 한꺼번에 넣으면 이벤트 큐가 넘쳐
일부가 유실되는데, 오류도 안 나고 로그도 안 남는다. 주기 스캔이 있으면 다음 사이클에
자동으로 주워 담는다 — **inotify는 어디까지나 체감 속도용이고, 정확성은 스캔이 보장한다.**

#### 하위 폴더는 평탄화

`incoming/` 아래에 폴더째로 드래그해도 재귀 탐색해서 전부 처리한다.
**원본의 폴더 구조는 보존하지 않는다** — 배치 기준은 EXIF 촬영일시다.
처리 후 빈 디렉터리는 정리한다. (폴더명을 앨범으로 쓰고 싶다는 요구가 나오면 v2에서 검토)

#### 라이브 포토 쌍이 갈라지는 문제

HEIC와 MOV는 별개 파일이라 **도착 시각이 다르다.** HEIC만 먼저 안정화되면
짝 없는 사진으로 처리될 수 있다. §6.5의 역방향 페어링(나중에 온 MOV를
`ContentIdentifier`로 뒤늦게 붙이기)이 이 상황을 흡수한다.
드롭 폴더에서는 이 케이스가 **흔하게** 발생하므로 선택이 아니라 필수다.

#### 일시적 실패와 영구적 실패를 구분한다 **[원칙]**

M1 구현 중 같은 성격의 문제를 네 번 만났다. 전부 **"실패의 종류를 구분하지 않은 것"**이
원인이었다. 이후 작업에서도 반복될 수 있어 원칙으로 남긴다.

| 지점 | 증상 |
|---|---|
| 인제스트 | DB 스키마가 아직 없다는 이유로 사용자 파일이 전부 격리됐다 |
| 파생물 | 권한 문제인데 `failed` 로 확정돼, 권한을 고쳐도 복구되지 않았다 |
| 재큐잉 | 중복 방지용 `job_id` 가 재시도까지 막았다 (arq 가 결과를 보관하는 동안) |
| 페어링 | 동시 인제스트로 짝을 못 찾은 뒤 다시 시도할 경로가 없었다 |

**두 가지를 항상 나눠 다룬다.**
- **파일 자체의 문제**(깨진 파일, 미지원 형식) → 격리하고 확정한다. 재시도해도 같다.
- **환경의 문제**(DB·권한·디스크·경합) → 원래 자리에 두고 **다음 주기에 다시 시도**한다.
  사용자 파일을 인프라 사정으로 잃거나 격리하면 안 된다.

**그리고 한 번의 시도로 끝내지 않는다.** 주기 스캔이 미완료 상태를 계속 주워 담는다 —
파생물이 `pending` 인 자산, 짝을 못 찾은 라이브 포토가 그 대상이다.
멱등하게 만들어 몇 번 돌아도 문제가 없게 한다.

#### 실패 처리 — 조용히 삼키지 않는다

실패한 파일은 `incoming/failed/`로 옮기고 사유를 남긴다.

- **`drop/`에 남겨두고 재시도하면 안 된다** — 매 사이클마다 같은 실패를 반복하며 로그를 채운다
- **삭제해도 안 된다** — 사용자가 올린 파일을 앱이 임의로 지우는 건 신뢰를 깬다
- 웹 UI에 실패 목록과 사유를 노출하고, 수정 후 다시 넣을 수 있게 한다

#### 진행 상황 노출 **[중요]**

사용자가 500장을 던져 넣고 나면 **아무 피드백이 없다.** 처리 중인지, 끝났는지,
실패했는지 알 수 없으면 같은 파일을 또 올리거나 폴더를 뒤지게 된다.

웹 UI 상단에 인제스트 상태를 상시 노출한다: `대기 N · 처리 중 N · 완료 N · 실패 N`.
클릭하면 최근 처리 이력과 실패 사유를 볼 수 있다.

#### 권한 — SFTP와 워커의 uid가 다르다

SFTP 사용자가 올린 파일은 그 사용자 소유가 되고, 워커는 다른 uid로 돌아
**읽거나 옮기지 못한다.** 드롭 폴더 구성에서 자주 걸리는 지점이다.

→ 공용 그룹(`poogiegram`)을 만들어 양쪽을 넣고, `incoming/`에 **setgid 비트**(`chmod 2775`)를
걸어 새 파일이 그룹을 상속하게 한다. SFTP 쪽 umask도 `002`로 맞춘다.

#### 업로드 경로 — 둘 다 지원, SFTP 먼저

| 방식 | 장점 | 단점 | 시점 |
|---|---|---|---|
| **SFTP** (Cyberduck/FileZilla) | **추가 서비스 불필요** — SSH가 이미 있다. 수천 장 일괄 전송에 강하고 중단·재개가 안정적 | 클라이언트 설치 필요. 폰에서 불편 | **M1** |
| **웹 업로드 페이지** | 폰·아이패드에서 바로. 설치 불필요. 진행률·실패를 같은 화면에서 확인 | 청크 업로드 구현 필요 (§6.6) | **M5** |
| SMB 공유 | 맥 파인더에서 네트워크 드라이브로 드래그 — 가장 자연스러움 | Samba 설치·인증 설정 추가 | 선택 |

**SFTP를 M1에 두는 이유는 코드가 0줄이기 때문이다.** 계정 하나 만들고 chroot하면 끝이라,
M1 인제스트 파이프라인을 **실제 아이폰 사진으로 바로 테스트**할 수 있다.

#### 사용자가 보는 것

접속하면 서버 전체가 아니라 드롭 폴더만 보인다. `originals/`·`trash/`·`db/`는
**chroot 밖이라 존재 자체가 보이지 않는다** — SFTP로 붙어서 원본을 지울 수 없다.

```
/                    ← 실제로는 /mnt/media/incoming
├── drop/            ← 여기에 올린다 (쓰기 가능한 유일한 곳)
└── failed/          ← 처리 실패분이 사유와 함께 나타난다
```

**처리가 끝나면 파일이 `drop/`에서 사라진다**(`originals/`로 이동). 드롭 폴더가
스스로 비워지는 것이 완료 신호라, 사용자가 따로 확인할 필요가 없다.

#### chroot 구성 — OpenSSH의 함정 두 가지

```
# /etc/ssh/sshd_config
Match User <drop-user>
    ChrootDirectory /mnt/media/incoming
    ForceCommand internal-sftp -u 0002
    AllowTcpForwarding no
    PermitTunnel no
    X11Forwarding no
```

```bash
sudo groupadd -f poogiegram
sudo useradd -M -d /drop -s /usr/sbin/nologin -G poogiegram <drop-user>   # 홈은 chroot 기준 경로

sudo chown root:root /mnt/media /mnt/media/incoming   # ← 함정 1
sudo chmod 755       /mnt/media /mnt/media/incoming

sudo mkdir -p /mnt/media/incoming/{drop,failed}       # ← 함정 2
sudo chown root:poogiegram /mnt/media/incoming/{drop,failed}
sudo chmod 2775            /mnt/media/incoming/{drop,failed}
```

**함정 1 — `ChrootDirectory`는 root 소유에 그룹·타인 쓰기 권한이 없어야 한다.**
아니면 접속 자체가 끊기고 로그에 `bad ownership or modes for chroot directory`가 남는다.
경로상의 **모든 상위 디렉터리**에 같은 조건이 적용된다(`/mnt`, `/mnt/media`도 포함).

**함정 2 — 그래서 chroot 대상에는 사용자가 쓸 수 없다.** 반드시 안에 쓰기 가능한
하위 디렉터리를 따로 둬야 한다. 이걸 모르면 "접속은 되는데 업로드만 `Permission denied`"
상태가 되고, 원인을 찾기 어렵다. `drop/`이 그 역할이다.

`-u 0002`(umask)와 setgid 비트(`2775`)가 함께 작동해, 올라온 파일이 `poogiegram` 그룹
소유가 되고 워커가 읽고 옮길 수 있게 된다.

SFTP는 **기본적으로 LAN 전용**이다. 외부 노출 여부와 그 방식은 §9 참조 —
**SSH를 외부에 여는 경우 키 인증 강제가 전제 조건**이다.

> **[열림] Q15.** 웹 업로드 페이지를 MVP(M4)까지 당길까요, M5로 둘까요?
> 폰에서 바로 올리는 경험이 중요하면 앞당길 만합니다. SFTP만으로도 기능은 완결됩니다.

**중요한 순서 변경**: 이전 초안은 "DB 행이 생기면 UI에 노출, 썸네일은 늦게 와도 됨"이었다.
HEIC에서는 **파생물이 없으면 크롬에서 아무것도 안 보이므로** 그 낙관이 깨진다.
→ UI는 `derive_status='pending'` 자산을 **자리표시자(스켈레톤)로 명시적으로 표시**하고,
   완료되면 교체한다. "빈 회색 칸이 왜 안 채워지지" 상태를 만들지 않는다.

### 6.2 사진 변환 — **전량 필수 경로** (정정됨)

브라우저의 HEIC 지원 현황이 설계를 규정한다.

| 브라우저 | HEIC 표시 |
|---|---|
| Safari (macOS/iOS) | 가능 |
| **Chrome / Firefox / Edge** | **불가** |

→ 아이폰 사진은 **예외 없이 `display.jpg` 파생물을 만들어야 한다.** 선택 사항이 아니다.

| 원본 | 처리 |
|---|---|
| HEIC / HEIF | `display.jpg`(장변 ~2560) + preview + thumb **필수 생성**. 원본은 다운로드용으로만 |
| JPEG (스냅 원본) | 원본 그대로 서빙 가능하나 20~40MB라 무겁다 → preview 우선 제공, 원본은 요청 시 |
| PNG 등 | 그대로 |
| RAW | **범위 제외** (§4.5). 들어오면 인제스트 거부 + 명확한 오류 메시지 |

- 변환 라이브러리: **`pillow-heif`** (libheif 바인딩) — Pillow에 HEIC 디코딩을 붙인다.
- **HDR 게인맵**: 아이폰 HEIC는 HDR 게인맵을 품는다. JPEG로 내리면 SDR로 눌린다.
  MVP에서는 **SDR 변환을 수용**하고, HDR 원본 표시는 v2로 미룬다. (브라우저 지원이 아직 고르지 않다)

### 6.3 동영상 전략 — **상시 트랜스코딩** (정정됨)

이전 초안은 "H.264가 대다수라 변환은 예외"였다. 아이폰 위주면 **HEVC가 다수**가 된다.

| 원본 상태 | 처리 |
|---|---|
| H.264 + AAC / MP4 | 변환 없이 Range 직접 서빙 |
| **HEVC (아이폰 기본)** | **HLS 트랜스코딩 — 이제 주 경로** |
| HEVC + Dolby Vision (최신 Pro) | HLS 트랜스코딩 **+ 톤매핑 필수** |

#### 10bit → 8bit 변환은 필수 경로다 **[확정]**

**아이폰 HEVC는 전부 10bit이고, 이 하드웨어의 H.264 인코드는 8bit만 지원한다**
(H.264 High10 엔트리포인트 없음). GPU 디코드 결과인 P010 서페이스를 그대로 인코더에
넘기면 **`No usable encoding profile found`로 실패한다.** 2026-08-07 실측으로 확인됨.

→ 필터 체인에 픽셀 포맷 변환을 반드시 포함한다.
```bash
-vf 'scale_vaapi=w=1920:h=1080:format=nv12'
```

#### 그런데 포맷 변환만으로는 부족하다 — 톤매핑

`format=nv12`는 **비트 깊이를 자를 뿐 톤매핑이 아니다.** SDR 소스는 이걸로 충분하지만,
Dolby Vision·HLG 영상에 그대로 적용하면 **색이 빠지고 허옇게 뜬다.**

| 방식 | 위치 | 이 하드웨어에서의 판단 |
|---|---|---|
| `tonemap_vaapi` | GPU | Gen9.5 지원 여부 불확실 — 확인 필요 |
| `tonemap_opencl` | GPU(OpenCL) | ffmpeg가 `--enable-opencl` 빌드. **1순위 후보** |
| `zscale`+`tonemap` | **CPU** | 항상 동작하지만 느리다. 최후 폴백 |

CPU 폴백 체인:
```
-vf zscale=t=linear:npl=100,tonemap=hable,zscale=t=bt709:m=bt709:r=tv,format=yuv420p
```

**HDR 여부에 따라 분기한다** — `asset.is_hdr`가 false면 `scale_vaapi=…:format=nv12`만으로
GPU 안에서 끝나고, true일 때만 톤매핑 경로를 탄다. 대부분의 사진·구형 영상은 전자에 해당한다.

> **[열림] Q18.** 어떤 톤매핑 경로를 쓸지는 M3에서 실측으로 정한다.
> `ffmpeg -filters | grep -i tonemap`과 Intel OpenCL 런타임 설치 여부로 후보를 좁힌다.
- 워커 동시 실행 수는 **2개로 시작**한다. HEVC 디코드·H.264 인코드가 모두 하드웨어로
  처리되고(§6.4) CPU는 6코어라 여유가 있다. 다만 GPU 엔진은 하나뿐이라 무한정 늘려도
  의미가 없다 — `TRANSCODE_CONCURRENCY`로 조절하며 실측한다.
- 톤매핑만 CPU를 쓰므로, 병목이 생기면 여기부터 확인한다.
- **시간대 제한(야간 배치) 스케줄러는 만들지 않는다.** 실측 3.53x 기준으로 2분짜리 4K
  클립이 34초, 100분 분량을 한꺼번에 던져도 약 28분이다. 그냥 백그라운드 큐에서
  순서대로 처리하면 충분하고, 스케줄링은 설정과 디버깅 대상만 늘린다.
  부하가 문제되면 `TRANSCODE_CONCURRENCY`를 낮추는 것으로 대응한다.
- **초기 대량 투입의 실제 병목은 영상이 아니라 HEIC 사진 변환이다.** 영상은 하드웨어가
  처리하지만 HEIC 디코딩은 CPU를 쓴다. §6.4의 내장 썸네일 즉시 추출이 중요한 이유다.

### 6.4 하드웨어 가속 **[제안]** — 중요도 상승

HEVC 디코드·인코드가 상시 경로가 되면서 **HW 가속이 사실상 필수**가 됐다.
소프트웨어 인코딩만으로 4K HEVC를 돌리면 미니PC에서 실시간의 몇 분의 1 속도가 나온다.

#### HuC 펌웨어가 필수다 **[확정]** — 하드웨어 인코드의 전제 조건

**이 항목을 빠뜨리면 하드웨어가 지원하는데도 인코드가 반쪽만 동작한다.**

Gen9.5(Coffee Lake)에서 i915는 `enable_guc` 기본값이 0이라 **GuC/HuC 펌웨어를 로드하지
않는다.** iHD 드라이버의 HEVC 인코드는 이 세대에서 HuC에 의존하므로, 펌웨어가 없으면
드라이버가 해당 엔트리포인트를 **아예 노출하지 않는다.** 하드웨어 한계처럼 보이지만 설정 문제다.

```bash
echo 'options i915 enable_guc=2' | sudo tee /etc/modprobe.d/i915.conf
sudo update-initramfs -u
sudo reboot
```

`enable_guc=2`는 **HuC 인증만 켜고 GuC 제출은 끈다.** Gen9 계열에서 GuC 제출(`=3`)은
안정성 이슈 이력이 있어, 미디어 인코딩 목적에는 `2`가 안전한 선택이다.
부팅 시 `Setting dangerous option enable_guc - tainting kernel` 경고가 뜨지만 무해하다.

확인:
```bash
sudo dmesg | grep -iE 'huc|guc'
# HuC firmware i915/kbl_huc_4.0.0.bin version 4.0 authenticated:yes   ← 이 줄이 나와야 함
```

#### 실측 확인 결과 **[확정]** — 2026-08-07, HuC 활성화 후

드라이버 **Intel iHD 22.3.1**(`intel-media-va-driver-non-free`), VA-API 1.14,
`/dev/dri/renderD128` 정상, HuC 4.0 authenticated.

| 기능 | 엔트리포인트 | 상태 |
|---|---|---|
| **HEVC Main10 디코드** | `VAEntrypointVLD` | ✅ **Dolby Vision 10bit 하드웨어 디코드** |
| HEVC Main 디코드 | `VAEntrypointVLD` | ✅ |
| **HEVC Main/Main10 인코드** | `VAEntrypointEncSlice` | ✅ **HuC 활성화로 확보** |
| H.264 디코드 | `VAEntrypointVLD` | ✅ |
| **H.264 인코드** | **`EncSlice` + `EncSliceLP` 둘 다** | ✅ **VME 경로 사용 — `-low_power` 불필요** |
| JPEG 디코드/인코드 | `VLD` / `EncPicture` | ✅ |
| VP9 / VP8 디코드 | `VAEntrypointVLD` | ✅ |
| **VPP (후처리)** | `VAProfileNone : VAEntrypointVideoProc` | ✅ **컨테이너에서 확인.** `scale_vaapi`가 여기서 돌고, `tonemap_vaapi`(Q18) 후보의 전제 |

**컨테이너 드라이버가 호스트보다 신형이다** — 호스트 iHD 22.3.1 / VA-API 1.14 대 컨테이너
**iHD 23.1.1 / VA-API 1.17**. bookworm이 Ubuntu 22.04보다 최신 media-driver를 담고 있다.
따라서 **가속 동작은 컨테이너 안에서 확인해야 한다**(`make vainfo`). 호스트 결과와 다를 수 있다.

**HEVC 디코드와 H.264 인코드가 모두 하드웨어로 처리된다. CPU가 담당하는 건 톤매핑뿐이다.**

#### 실측 성능 **[측정값]** — 2026-08-07

**4K HEVC Main10 → 1080p H.264**, GPU 디코드 → GPU 인코드, 톤매핑 없음:

| 항목 | 값 |
|---|---|
| 처리 속도 | `fps=107`, **`speed=3.53x`** |
| 1분 클립 환산 | 약 **17초** |
| 출력 비트레이트 | 6502 kb/s (요청 6000) — **레이트 컨트롤 정상 동작** |

비트레이트가 요청값에 근접한 것이 **HuC가 제 역할을 한다는 증거**다. HuC 없이 CQP로
떨어졌다면 이렇게 맞지 않는다.

§4.4의 영상 200분 기준으로 **전체 변환에 약 1시간** — 초기 대량 투입도 한 번에 끝난다.

**단서 두 가지:**
- **톤매핑이 빠진 값이다.** Dolby Vision 클립은 여기에 톤매핑 비용이 더해진다.
  CPU 경로(`zscale`+`tonemap`)로 떨어지면 크게 느려질 수 있다 → Q18
- **워커를 2개로 늘려도 2배가 되지 않는다.** GPU 인코드 엔진은 하나뿐이라
  디코드/인코드는 직렬화된다. 병렬화 이득은 CPU가 담당하는 구간(톤매핑, 컨테이너
  먹싱, HEIC 변환)에서만 나온다.

#### 인코드 엔트리포인트 — VME 사용, `-low_power` 불필요

HuC 활성화 후 H.264에 `EncSlice`(VME)와 `EncSliceLP`(VDEnc)가 모두 열렸다.
**VME 경로가 같은 비트레이트에서 화질이 더 좋으므로 기본값(EncSlice)을 쓴다.**

```bash
ffmpeg -hwaccel vaapi -hwaccel_device /dev/dri/renderD128 \
       -hwaccel_output_format vaapi -i input.mov \
       -c:v h264_vaapi -b:v 6M \
       -c:a aac -b:a 128k output.m3u8
```

**단, 하드웨어에 강결합하지 않는다.** 서버를 바꾸거나 HuC 설정이 풀리면 `EncSlice`가
사라질 수 있다. 그 환경에서 `-low_power 1` 없이 실행하면
`No usable encoding entrypoint found for profile`로 실패한다 — 가속이 되는데도 안 된다고
헤매기 쉬운 지점이므로, **기동 시 엔트리포인트를 탐지해 `-low_power`를 자동으로 붙인다.**

HuC는 **비트레이트 제어(CBR/VBR)에도 필요하다.** 없으면 CQP 위주로 동작해 HLS 세그먼트
비트레이트가 들쭉날쭉해진다. 이것이 HEVC 인코드 확보보다 실질적으로 중요한 이득이다.

> **[참고] HEVC 인코드 활용 여지 (v2)** — 현재 설계는 브라우저 호환성 때문에 H.264로만
> 출력한다. 다만 사파리·iOS는 HEVC를 네이티브 재생하므로, HLS 마스터 플레이리스트에
> **HEVC 변형(variant)을 추가**하면 같은 화질에서 대역폭·용량을 아낄 수 있다.
> 아이폰 위주 사용 환경이라 실익이 있으나, 인코딩 시간과 저장 용량이 배로 드는
> 트레이드오프가 있어 v2에서 검토한다.

#### 컨테이너에서 GPU 쓰기 — render GID가 필요하다

`/dev/dri/renderD128`은 `root:render 660`이라, 장치를 넘기는 것만으로는 부족하고
**컨테이너 프로세스가 render GID를 가져야** 한다.

```yaml
services:
  worker:
    devices:
      - /dev/dri:/dev/dri
    group_add:
      - "${RENDER_GID}"        # getent group render | cut -d: -f3
```

호스트마다 GID가 다르므로 `.env`로 주입한다. 이걸 빠뜨리면 컨테이너 안에서만
`Permission denied`가 나고, 호스트에서는 잘 되기 때문에 원인을 찾기 어렵다.

**서버 CPU는 Coffee Lake(8세대) — 이 워크로드에 필요한 사양을 충족한다.**

| 세대 | HEVC 8bit 디코드 | HEVC 10bit 디코드 | 판단 |
|---|---|---|---|
| 6세대(Skylake) 이하 | 부분 | 불가 | 소프트웨어 폴백 각오 |
| 7세대(Kaby Lake) | 가능 | 가능 | 실용 하한선 |
| **8세대 (Coffee Lake) ← 현재 서버** | 가능 | **가능** | **충족** |

UHD Graphics 630이 HEVC Main/Main10 디코드와 인코드를 모두 지원한다.
Dolby Vision 10bit 디코딩까지 하드웨어로 처리되므로 §6.3의 톤매핑 부하가 크게 줄어든다.
6코어라 HEIC 배치 처리도 병렬로 돌릴 수 있다.

- 가속 가능 여부는 **런타임 탐지 + libx264 소프트웨어 폴백**. 하드웨어에 강결합하지 않는다.
- 환경변수 `TRANSCODE_HWACCEL=vaapi|none|auto`, `TRANSCODE_CONCURRENCY=2`, `RENDER_GID`.
- AMD는 VA-API 그대로, NVIDIA는 NVENC로 인코더 이름만 분기.

**다른 서버로 옮길 때의 검증 절차** — 아래 네 가지를 순서대로 확인한다.
```bash
ls -l /dev/dri/                                              # renderD128 존재
sudo dmesg | grep -iE 'huc|guc'                              # HuC authenticated:yes
sudo vainfo --display drm --device /dev/dri/renderD128       # 엔트리포인트 목록
getent group render                                          # 컨테이너에 넘길 GID
```
`vainfo`는 헤드리스에서 `--display drm` 없이 실행하면 X 서버를 찾다가 실패한다.
`HuC authenticated:yes`가 없으면 인코드 엔트리포인트가 반쪽만 나오므로
위 HuC 설정을 먼저 적용한다.

### 6.5 라이브 포토 **[확정]** — 살린다

아이폰은 `IMG_1234.HEIC` + `IMG_1234.MOV`를 **쌍으로** 내보낸다. 그냥 넣으면 타임라인에
같은 장면이 사진 1장 + 영상 1개로 두 번 뜬다.

**페어링 키 — 필드 이름이 양쪽에서 다르다 [실측]**

| 파일 | 필드 | 값 |
|---|---|---|
| 정지컷(HEIC) | `MakerNotes:MediaGroupUUID` | 동일 |
| 동반 MOV | `QuickTime:ContentIdentifier` | 동일 |

값은 같은데 이름이 다르므로 **둘 다 읽어야 한다.** (2026-08-08 아이폰 16 Pro 실측)

**주의 — 사진앱에서 내보낸 JPEG 도 같은 UUID 를 갖는다.** HEIC·JPEG·MOV 셋이 한 UUID 를
공유하므로 "같은 ID 면 한 묶음"이 아니라 **영상 하나에 정지컷 여럿이 붙는 형태**로 다룬다.

**영상에 이 값이 있으면 그 자체로 동반 클립이다** — 애플은 라이브 포토에만 쓴다.
짝이 아직 안 왔어도 즉시 숨김 처리한다. 드롭 폴더에서는 MOV 가 먼저 도착하는 일이
흔한데, 짝을 찾은 뒤에 판정하면 그 사이 3초짜리 영상이 타임라인에 노출된다.

매칭되면 MOV 쪽에 `is_live_motion=true`를 세우고 타임라인에서 숨긴다.
라이트박스에서 **길게 누르면 모션 재생** — 아이폰과 같은 동작.

**모션 MOV의 재생 처리**: 3초 내외로 짧고 용량이 작으므로 **HLS 변환 대상에서 제외**한다.
HEVC라도 그대로 두고, 재생이 안 되는 브라우저에서는 정지컷만 보여준다.
(짧은 클립 하나하나를 HLS로 쪼개면 세그먼트 파일만 수십만 개가 되어 파일시스템이 괴로워진다.)

**업로드 시 주의**: 사용자가 HEIC만 올리고 MOV를 빠뜨리는 경우가 흔하다.
업로드 UI에서 **쌍이 맞지 않는 파일을 감지해 안내**한다. 나중에 MOV가 들어와도
`ContentIdentifier`로 뒤늦게 페어링되도록 인제스트에서 역방향 조회를 한다.

### 6.6 웹 업로드 **[확정]**

**아이폰에서 사진을 넣을 방법이 이것뿐이다.** SFTP는 맥에서만 되고, 그러면 결국 "남편에게
부탁해서 넣기"가 되어 아무도 안 쓰게 된다. 기능이 있어도 마찰이 크면 안 쓰인다.

**청크 업로드는 넣지 않았다.** 실제 사용이 기념사진 수십~수백 장으로 정해지면서(§2) 파일
하나가 큰 경우가 드물어졌다. 이어올리기가 필요해지는 것은 GB 단위 영상인데, 그때 얹으면 된다.

- **파일 하나당 요청 하나.** 한 요청에 묶으면 하나가 실패했을 때 무엇이 들어갔는지 알 수 없고
  진행률도 파일 단위로 못 보여준다. 동시 2개까지만 — 모바일 회선에서 여러 개를 한꺼번에 밀면
  전부 느려지고 타임아웃만 는다
- **진행률은 XHR로만 얻을 수 있다.** `fetch`는 업로드 진행률을 주지 않는다. 진행률이 없으면
  큰 파일에서 멈춘 건지 올라가는 중인지 몰라 사용자가 취소해버린다
- nginx `client_max_body_size`는 **가장 큰 파일 하나**를 담아야 한다 (§3.2). 넘으면 nginx가
  413을 내고 앱 로그에는 아무것도 남지 않는다
- **태그는 폴더 규칙을 재사용한다** (§5.5). 고른 태그 이름으로 드롭 폴더 안에 폴더를 만들면
  인제스트가 알아서 붙인다. 새 경로를 만들지 않는 편이 검증된 동작을 재사용하는 길이다
- 태그·파일명은 **사용자가 정한 값이 그대로 경로가 된다.** 경로 구분자·제어문자를 치환하고
  NFC로 모은다 — 맥·아이폰이 한글을 자모 분리(NFD)로 보내면 같은 태그가 둘로 갈라진다
- `.partial`로 쓰고 다 받은 뒤 rename한다. 스캐너가 이 확장자를 무시하므로 전송 중인 파일을
  잘린 채로 집어가지 않는다
- 업로드 = 파일 수신까지만. **해시·변환은 전부 큐로 넘긴다.** HTTP 요청 안에서 ffmpeg를 돌리면
  타임아웃과 워커 점유로 서비스가 멎는다.
- 클라이언트에서 **업로드 전 SHA-256 선계산 → 서버에 조회** → 이미 있으면 전송 자체를 생략.
  같은 사진을 두 번 올리는 일이 잦으므로 효과가 크다. **[미구현]** — 지금은 그냥 올리고
  인제스트가 해시로 중복을 걸러낸다. 대역폭이 아깝지만 규모가 작아 체감되지 않는다.

### 6.7 촬영일 판정 **[제안]** — 날짜가 깨진 파일 다루기

**스냅사진 납품본은 촬영일이 깨져 있는 경우가 흔하다.** 작가의 내보내기 과정에서 EXIF가
지워지거나, ZIP으로 전달되며 타임스탬프가 유실된다.

> **`1980-01-01`은 MS-DOS/ZIP 에폭이다.** 이 날짜가 보이면 "ZIP으로 받아서 압축을 풀 때
> 타임스탬프가 보존되지 않았다"는 뜻이다. `1970-01-01`(유닉스 에폭)도 같은 성격이다.
> 진짜 촬영일이 아니라 **정보가 없다는 신호**로 읽어야 한다.

그냥 두면 이 사진들이 `originals/1980/01/01/`에 쌓여 타임라인 끝에 뭉치고,
"과거의 오늘"에서는 1월 1일마다 튀어나온다.

#### 우선순위 체인

위에서부터 시도해 **처음 성공하는 값**을 쓰고, 그 출처를 `date_source`에 기록한다.

| 순위 | `date_source` | 출처 | 비고 |
|---|---|---|---|
| 1 | `exif` | EXIF `DateTimeOriginal` | 가장 신뢰 |
| 2 | `exif` | EXIF `CreateDate` / `ModifyDate` | |
| 3 | `xmp` | XMP 메타데이터 | **라이트룸 내보내기에서 EXIF가 지워져도 살아남는 경우가 많다** |
| 4 | `filename` | 파일명 패턴 | `20240315_001.jpg`, `2024-03-15_1234.jpg` — **스냅사진에서 잘 먹힌다** |
| 5 | `sibling` | 같은 배치의 형제 파일 | 200장 중 190장이 정상이면 나머지도 같은 촬영으로 추정 |
| 6 | `mtime` | 파일 수정 시각 | 최후 수단. 신뢰도 낮음 |
| — | `unknown` | 전부 실패 | `_undated/`로 |

#### 타당성 검사

각 단계에서 얻은 값이 **1995년 이전이거나 현재보다 미래면 버리고 다음 순위로 넘어간다.**
카메라 시계가 초기화돼 `2000-01-01`이 박히는 경우도 이 검사에 걸린다.
`1980-01-01` / `1970-01-01`은 명시적으로 무효 처리한다.

#### 판정 실패 시 — 거부하지 않고 표시한다

500장짜리 납품을 통째로 반려하면 쓸모없는 설계다. **들여오되 구분한다.**

- 경로는 `originals/_undated/` — 1980년을 오염시키지 않는다
- `taken_local`은 NOT NULL이므로 인제스트 시각을 넣되 `date_source='unknown'`으로 표시
- 타임라인에서 **별도 섹션으로 분리**한다 (날짜 순서에 섞지 않음)
- **"과거의 오늘"에서 제외**한다 — `date_source IN ('mtime','unknown')` 배제.
  스크린샷을 거르는 것과 같은 이유다

#### 나중에 고칠 수 있게 한다

웹 UI에서 **여러 장을 선택해 한 번에 촬영일을 지정**할 수 있게 한다
("이 200장은 2024년 3월 15일"). 스냅사진은 배치로 들어오므로 배치로 고치는 게 자연스럽다.

날짜가 확정되면:
1. `taken_local` / `taken_at` 갱신, `date_source='manual'`
2. **파일을 `_undated/`에서 `originals/2024/03/15/`로 이동** (같은 파일시스템 → `rename()`)
3. `asset.path` 갱신

§4.1의 "원본은 건드리지 않는다"는 **내용**에 대한 원칙이고, 위치 이동은 허용된다.

> **[열림] Q20.** 배치 날짜 지정 UI를 언제 넣을까요? 스냅사진 비중이 크면 M4로,
> 아이폰 사진 위주면 M5로 미뤄도 됩니다. 스키마(`date_source`)와 `_undated/` 경로는
> M1에 포함되므로, UI만 나중에 붙여도 데이터는 안전하게 쌓입니다.

---

## 7. 웹 프런트엔드 **[제안]**

- **React + TypeScript + Vite**
- **TanStack Query** — 서버 상태 캐싱 (무한 스크롤과 궁합이 좋음)
- **가상 스크롤 필수** — 수만 장에서 DOM을 다 그리면 브라우저가 멈춘다. UI 톤과 무관하게 필요.
- 썸네일은 `loading="lazy"` + 저해상도 placeholder로 로딩 체감 개선
- 라이트박스: 키보드(←→, Esc), 모바일 스와이프, 핀치 줌
- 비디오: `hls.js` (Safari는 네이티브 HLS)

### 7.1 갤러리형 레이아웃 **[확정]**

밀도형(구글 포토식 꽉 찬 그리드)이 아니라 **여백 있는 감상 중심** 배치로 간다.

| 항목 | 방향 |
|---|---|
| 배치 | *Justified layout* — 행 높이를 맞추고 가로폭을 채우되, **간격(gutter)을 넉넉히**(12~16px) |
| 행 목표 높이 | 240~320px (밀도형의 통상 140~180px보다 크게) — 한 장 한 장이 읽힌다 |
| 날짜 구분 | 월 단위 헤더 + 위아래 큰 여백. 스크롤에 리듬을 준다 |
| 배경 | 중립 그레이(다크 우선). 사진 색을 방해하지 않는 것이 최우선 |
| 크롬(UI 장식) | 최소화. 호버 시에만 메타 정보 노출 |

**단, 여백형은 한 화면에 담기는 장수가 적어 스크롤이 길어진다.** 이를 보완할 장치:
- **연도·월 점프 스크럽바** (우측 고정) — 수만 장에서 원하는 시기로 즉시 이동
- 그리드 밀도를 사용자 설정으로 3단계(여유/보통/조밀) 제공, 기본값은 여유

이 구조면 나중에 밀도형이 필요해져도 CSS 변수(행 높이·gutter)만 바꾸면 된다.

---

## 8. 인증 · 공유 **[제안]**

- 세션: **httpOnly + SameSite=Lax 쿠키** (Redis 저장). JWT는 이 규모에서 로그아웃 처리만 번거로워진다.
- 역할: `admin`(전체 관리·사용자 추가) / `member`(본인 업로드 + 공유받은 것)
- 공유 링크: 서명된 랜덤 토큰, **만료일·비밀번호·다운로드 허용 여부** 선택 가능
- 회원가입 개방 없음 — admin이 계정을 발급한다.

---

## 9. 외부 접속 **[상세는 비공개 문서]**

기본 원칙만 여기 남긴다. **실제 노출 방식·포트·하드닝 절차는
`docs/environment.local.md`에 둔다** — 어떤 포트가 어떻게 열려 있고 어떤 계정이
있는지는 공개할 성격이 아니다.

- 웹은 **호스트 nginx(80/443)를 재사용**한다. 새 리버스 프록시를 띄우지 않는다 (§3).
- SFTP 드롭 폴더(§6.1)는 **기본적으로 LAN 전용**이다. M0·M1은 전부 LAN 안에서 끝난다.
- **SSH를 외부에 노출한다면 키 인증 강제(`PasswordAuthentication no`)가 전제 조건**이다.
  웹 앱은 컨테이너에 격리돼 있지만 SSH가 뚫리면 서버 전체다.
  끄기 전에 키 로그인이 되는지 반드시 먼저 확인한다 — 순서를 바꾸면 스스로 잠긴다.
- 웹 업로드 페이지(M5)가 들어오면 **외부 SFTP는 선택 사항이 된다.**

## 10. 개발 로드맵

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| ~~**M0**~~ | 저장소·Docker Compose 골격, 헬스체크 | ✅ **완료** — 전 스택 기동, readyz 통과 |
| ~~**M1**~~ | 드롭 폴더 인제스트 + HEIC→display.jpg + 썸네일 (UI 없음) | ✅ **완료** — 실측 검증 (라이브 포토 페어링 포함) |
| **M2** | 인증 + 타임라인 그리드 + 라이트박스 | 로그인 후 아이폰 사진을 브라우저에서 본다 |
| **M3** | 동영상 (HEVC HLS 트랜스코딩 + **Dolby Vision 톤매핑**) + HW 가속 | 아이폰 4K 영상이 색 왜곡 없이 재생된다 |
| **M3.5** | 라이브 포토 페어링 **[확정 범위]** | 사진 1장으로 표시되고 길게 눌러 모션 재생 |
| **M4** | 앨범·태그·검색 | MVP 완료 |
| **M5** | **웹 업로드 페이지**(청크), 공유 링크, 휴지통, `pg_dump` 자동화, 용량 경보 | 실사용 투입 |
| v2 이후 | **과거의 오늘·추억**(§5.1 — 스키마는 M1에 포함), HDR 게인맵 표시, 지도 뷰, 얼굴 인식, 중복 정리, PWA | |

M1을 먼저 하는 이유: 데이터가 있어야 UI를 제대로 만들 수 있다. 반대로 하면 더미 데이터에
맞춘 화면을 나중에 다시 짜게 된다.

**아이폰 정정으로 M1의 비중이 커졌다.** HEIC 변환이 M1에 들어가지 않으면 M2에서 화면에
아무것도 안 뜬다 — 이전처럼 "썸네일은 나중에"로 미룰 수 있는 항목이 아니다.
반대로 M1만 제대로 되면 M2는 순수 프런트엔드 작업으로 깔끔하게 분리된다.

---

## 11. 열린 질문 정리

### 해소됨
| # | 질문 | 결론 |
|---|---|---|
| ~~Q1~~ | 기존 라이브러리 유무 | **없음** — 앱이 전 파일 소유, 마이그레이션 불필요 (§4.3) |
| ~~Q2~~ | HEVC 비중 | **정정됨 → 높음.** 최신 아이폰 Pro 위주 + 스냅 원본. 변환이 주 경로 (§2.1) |
| ~~Q3~~ | UI 톤 | **갤러리형** — 여백 있는 justified layout (§7.1) |
| ~~Q4~~ | 라이브러리 규모 | **수천~수만 장** — 단일 PostgreSQL로 여유. 용량은 §4.4에서 재산정 |
| ~~Q5~~ | 서버 하드웨어 | **Intel 리눅스 서버** — VA-API 가속 전제 (§6.4) |
| ~~Q11~~ | 스냅사진 RAW 여부 | **JPEG 납품본만** — RAW 현상 파이프라인 범위 제외 (§4.5) |
| ~~Q12~~ | 라이브 포토 모션 | **살린다** — 페어링 + 길게 눌러 재생 (§6.5) |

### 남은 질문
| # | 질문 | 영향 범위 | 지금 막히는가 |
|---|---|---|---|
| Q6 | ~~Intel 몇 세대인가?~~ | **해소** — 8세대, HEVC 10bit 가속 지원 (§6.4) | — |
| **Q17** | **사용할 도메인 / 서브도메인은?** | nginx 서버블록·인증서 발급 | **M2에서 막힌다** (M0~M1은 로컬 포트로 가능) |
| Q18 | HDR 톤매핑을 어느 경로로? (vaapi / opencl / CPU) | M3 트랜스코딩 성능 | 아니오 — M3에서 실측 결정 (§6.3) |
| Q15 | 웹 업로드 페이지를 M4로 당길까, M5로 둘까? | 로드맵 순서 | 아니오 — SFTP로 M1~M4 커버 (§6.1) |
| **Q8** | **원본 복제본을 어디에 둘 것인가?** (로컬 백업 철회로 현재 백업 없음) | 데이터 안전성 | 아니오 — 단 §4.2의 위험 구간이 계속 열려 있다 |
| ~~Q9 / Q19~~ | ~~외부 접속 방식~~ | **해소** — 상세는 비공개 문서 (§9) | — |
| ~~Q10~~ | ~~프로젝트 이름~~ | **해소 — `poogiegram`** | — |

**해소된 항목**
- **Q7**(디스크 구성) — 내장 NVMe + 외장 HDD 2대. 배치는 §4.0
- **Q13**(파일시스템) — 두 디스크 모두 ext4. `sdb1`은 초기화 후 사용
- **Q14**(`sdb1` 기존 데이터) — **전량 불용, 폐기 확정.** 2TB 전체 사용 가능 (§4.6)

**M1을 막는 블로커는 없다.** 입력 포맷·하드웨어·스토리지가 모두 확정됐다.
남은 건 M0을 막는 Q16(포트 점유)과 Q10(이름) 둘뿐이다.

### 착수 전 체크리스트

| # | 작업 | 상태 | 참조 |
|---|---|---|---|
| 1 | `sdb1` 초기화 (`mkfs.ext4`) | ✅ 완료 | §4.6 |
| 2 | `tune2fs -m 1` 예약 블록 회수 | ✅ 완료 (백업 +19GB) | §4.6 |
| 3 | fstab UUID + `nofail`, `/mnt/media`·`/mnt/backup` 마운트 | ✅ 완료 (`findmnt --verify` 통과) | §4.6 |
| 4 | 디렉터리 골격 + `.poogiegram-ok` 마커 | ✅ 완료 | §4.1 |
| 5 | 재부팅 후 마운트 유지 검증 | ✅ 완료 | §4.6 |
| 6 | 기존 80/443 점유 확인 → 프록시 구성 확정 | ✅ 완료 — 호스트 nginx 재사용 (§3) | §3 |
| 7 | `/dev/dri` + `vainfo` VA-API 동작 확인 | ✅ 완료 — HEVC 10bit 디코드 + H.264 LP 인코드 | §6.4 |
| 8 | `poogiegram` 그룹 + setgid 권한 구성 | M1 | §6.1 |
| 9 | `smartmontools` 설치 및 주기 테스트 | M1 | §4.6 |
| 10 | 프로젝트 이름 확정 | ✅ **`poogiegram`** | — |
| 11 | 디스크 라벨·마커 파일 이름 통일 (아래 명령) | 대기 | §4.6 |

이름 확정에 따른 서버 측 정리 (선택이지만 지금 해두면 깔끔하다):
```bash
sudo e2label /dev/sdb1 poogiegram
sudo mv /mnt/media/originals/.mediavault-ok /mnt/media/originals/.poogiegram-ok
sudo mkdir -p /mnt/media/db /mnt/media/.tmp
sudo rmdir /mnt/backup/originals /mnt/backup/pg 2>/dev/null   # 백업 계획 철회 (§4.2)

# 드롭 폴더 재구성 (§6.1 — 기존 incoming/{_failed,_tmp} 대체)
sudo rmdir /mnt/media/incoming/_failed /mnt/media/incoming/_tmp 2>/dev/null
sudo mkdir -p /mnt/media/incoming/{drop,failed}
```
라벨은 fstab이 UUID를 쓰므로 기능에 영향이 없다. 마커 파일은 앱이 기동 시 확인하므로
코드와 이름이 일치해야 한다(§4.6).

**스토리지 구성은 2026-08-07 완료.** 재부팅 후에도 마운트가 유지되고 마커 파일이 정상 확인됨.
디렉터리는 현재 `root:root` 소유이며, SFTP 계정과 워커의 공동 접근 권한은 M1에서 구성한다(#8).

---

## 12. 문서 규칙

- 이 파일(`PROJECT.md`)이 **단일 기준 문서**. 결정이 바뀌면 여기부터 고친다.
- 세부 설계는 `docs/` 아래로 분리하고 여기서 링크한다.
- 개발 착수 시 `CLAUDE.md`를 별도로 만들어 코딩 규칙·명령어를 정리한다.
